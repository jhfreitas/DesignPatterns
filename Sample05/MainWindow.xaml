<Window x:Class="Sample05.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sample05"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="dgCommand" HorizontalAlignment="Stretch" Margin = "5" VerticalAlignment="Stretch">
            <DataGrid.Columns>
                <!--<DataGridTextColumn x:Name="dgcDateTime" Binding="{Binding DateTime}" Header="Data/Hora"/>
                <DataGridTextColumn x:Name="dgcCommand" Binding="{Binding CommandReceived}" Header="Commando Recebido"/>-->
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
